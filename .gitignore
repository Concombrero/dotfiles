# Install log
install.log

# Machine-specific local overrides (not tracked)
**/local.conf
git/.gitconfig.local
fish/.config/fish/local.fish

# OpenCode local secrets (not tracked)
**/firecrawl_api_key

# OS files
.DS_Store
Thumbs.db

# Neovim local runtime/session state
nvim/.config/nvim/sessions/
nvim/.config/nvim/lazy-lock.json

# alacritty (legacy terminal emulator, using kitty now)
alacritty/
