# ============================================
# Dotfiles - APT Packages (Ubuntu/Debian)
# ============================================
# Only packages that are well-maintained in the Ubuntu repos.
# Packages installed via other methods (PPA, binary, source) are
# handled directly in install.sh.

# Core tools
git
stow
curl
wget
unzip
bc
build-essential
cmake
pkg-config
xdg-utils

# Window Manager & Desktop
i3-wm
i3lock
i3status
polybar
picom
rofi
dunst
flameshot
xdotool
x11-xserver-utils
x11-utils
dex
xss-lock
network-manager-gnome
pulseaudio-utils
imagemagick

# CLI Tools
htop
neofetch
ripgrep
fd-find
bat
feh
jq

# Document / Academic
zathura
zathura-pdf-poppler
texlive-full
latexmk

# Image viewer
sxiv

# Python (needed by Neovim plugins: pyright, black, isort, pylint, jupytext, ipython)
python3
python3-pip
python3-venv

# Node.js build dependencies (needed by nvim markdown-preview, mason LSP servers)
# Actual Node.js is installed via NodeSource in install.sh

# Other
vlc
