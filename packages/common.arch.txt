# Arch package names verified against the official package database:
# https://archlinux.org/packages/

# Core Tools
git
github-cli
stow
tmux
curl
wget
unzip
bc
xdg-utils
btop
ripgrep
fd
bat
jq
tldr
nodejs
npm

# Build Essentials
base-devel
cmake
pkgconf
# Use rustup to provision the current stable toolchain; tree-sitter CLI is
# installed via cargo in install/tools.sh because Arch's package can lag the
# version required by nvim-treesitter's `main` branch.
rustup
python
python-pip
# Arch's `python` package already ships the stdlib `venv` module.

# Runtime libraries for upstream CLI tools
alsa-lib

# Use official Arch packages when available instead of upstream installers.
# `neovim` is installed from the upstream release archive in install/tools.sh.
# `sesh` is installed separately from the AUR package `sesh-bin` in install/tools.sh.
fzf
starship
zoxide
yazi
lazygit
opencode
typst
python-pipx

# Shell
zsh
fish
